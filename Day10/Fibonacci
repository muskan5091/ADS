class Solution {
        public int fib(int n) {

         if(n==0)
        return 0;
        else if(n==1)
        return 1;
        return fib(n-1)+fib(n-2);


        // if(n==0)
        // return 0;
        // if(n==1)
        // return  1 ;
        // int firstT=0;
        // int secondT=1;
        // for(int i=0;i<n;i++){
        //     int thirdT=firstT+secondT;
        //     firstT=secondT;
        //     secondT=thirdT;

        // }
        // return firstT;

       
    }
}
